# Secrets — never commit (values live in Vercel / cloud secret manager)
secrets.md
.env
.env.*
!.env.example

# Dependencies / build
node_modules/
.vercel/

# OS / editor
.DS_Store
Thumbs.db

# Local Claude Code settings (machine-specific)
.claude/

# Logo iteration backups (working scratch)
images/brand/_backup_*/
